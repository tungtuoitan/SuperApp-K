---
name: "generic-nullable"
---

# lí do generic type tồn tại?
cho phép viết class/method hoạt động với nhiều kiểu dữ liệu

# generic tương ứng với gì trong JS?
k có

# nullable type là gì?
cho phép value type (int, bool...) nhận giá trị null bằng cú pháp `int?`. Là shorthand của `Nullable<T>`.
