---
id: 337
name: "iterate-loop"
---

# LINQ là gì? [id:3189 order:1]
là cú pháp query collection tương tự SQL

# LINQ giống sql ở chỗ nào? [id:3190 order:2]
giống ở cú pháp

# interate có phải là 1 loại loop không? hay là 1 lần lặp của loop? [id:3379 order:3]
Không
Iterate là hành động duyệt qua collection — có thể dùng loop hoặc không (LINQ, enumerator).

# interate là hành động hay là cơ chế? [id:3380 order:4]
là hành động (v).
Loop mới là cơ chế (n).

# interate là gì? [id:3381 order:5]
là (v) duyệt qua collection

# so sánh loop và interate? [id:3382 order:6]
loop là cơ chế (n), interate (v) là 1 hành động cụ thể

# trong loop k nhất thiết phải có interate phải k? [id:3198 order:7]
Đúng.