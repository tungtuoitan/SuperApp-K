---
id: 311
name: ".NET"
---

# .NET là gì? [id:2690 order:1]
là nền tảng phát triển phần mềm của Microsoft, gồm runtime (CLR) và thư viện chuẩn (BCL), dùng để build app console, web, desktop, mobile.

# namespace trong .NET là gì? [id:2797 order:2]
là 1 nhóm các class/interface

# assembly là gì? [id:2798 order:3]
là đơn vị deploy của .NET — thường là file `.dll` hoặc `.exe`, chứa IL và metadata của 1 project hoặc library.

# đơn vị deploy là gì? [id:2799 order:4]
là phần nhỏ nhất mà bạn copy/ship lên server để app chạy. Trong .NET là file `.dll`/`.exe`.

# đơn vị deploy hầu hết là file phải không? [id:2800 order:5]
Đúng, nhưng cũng có thể là folder, container image (Docker), hoặc package (`.nupkg`, `.zip`) tùy cách ship.

# ship là gì? [id:2801 order:6]
là đóng gói và đưa code/sản phẩm lên môi trường thật để user dùng — có thể là deploy server, publish package, push image lên registry.

# value type và reference type khác nhau gì?
Value type lưu giá trị ở stack hoặc inline trong object. 
Reference type lưu địa chỉ trỏ đến object trên heap 

# stack và heap khác nhau gì?
Stack: 
    là vùng nhớ nhỏ, nhanh
    chứa local variable và value type. 
    tự dọn khi function return 
Heap là vùng nhớ lớn, do GC quản lý
    chứa reference type, 
    sống lâu hơn scope.
# độ lớn giữa stack và heap?
# stringbuilder là gì?
# stringbuiler dùng làm gì?

# boxing và unboxing là gì?
Boxing: convert value type → object (wrap vào heap). Unboxing: ngược lại, lấy value type ra khỏi object. 
# boxing, unboxing k phổ biến phải không?

# async/await dùng để làm gì?
để io mà k tốn thread
# ngoài io thì await có dùng làm gì nữa không?
# mỗi process có 1 thread pool phải không?
# thread của OS khác gì với Thread trong .NET?
# Task khác Thread như thế nào?
Task:
    dùng cho short running
    chạy trên thread pool
Thread: 
    dùng cho long running
    là OS thread
# 1 Thread trong thread pool có tương ứng với 1 OS thread không?
# Garbage Collection trong .NET hoạt động thế nào?
GC tự dọn object không còn reference trên heap. Chia 3 generation (0, 1, 2) — object sống lâu được "promote" lên generation cao hơn để dọn ít thường xuyên hơn.
# Task liên hệ gì với thread pool?
# interface khác abstract class thế nào?
Interface chỉ định nghĩa contract (signature), 1 class implement nhiều interface. Abstract class có thể có code, field, constructor — chỉ kế thừa được 1.
# khi nào dùng abstract class?
# các ví dụ phổ biến dùng abstract class?
<!-- # override và overload khác nhau gì?
Overload: cùng tên method, khác parameter (cùng class). Override: con override method của cha (cần `virtual` ở cha, `override` ở con). -->
# overload là loại từ gì?
vừa là danh từ, vừa là động từ
# overload(n) là gì?
là hàm trùng tên nhưng khác params
# overload được tạo ra khi nào?
# overide (v) là gì?
là việc lớp con thay đổi implementation của lớp cha
# cần keyword gì để overide(v)?
# const, readonly, static khác nhau gì?
- `const`: compile-time constant, gán lúc khai báo, ngầm là `static`.
- `readonly`: gán lúc khai báo hoặc trong constructor, fix lúc runtime.
- `static`: thuộc class chứ không thuộc instance.
<!-- 
# ref và out khác nhau gì?
Cả hai pass by reference. `ref`: biến phải khởi tạo trước khi truyền. `out`: không cần khởi tạo, nhưng method bắt buộc gán giá trị trước khi return. -->

<!-- # IEnumerable, IList, IQueryable khác nhau?
- `IEnumerable<T>`: chỉ enumerate forward, lazy, in-memory.
- `IList<T>`: thêm index access, Add/Remove, in-memory.
- `IQueryable<T>`: build expression tree, dịch sang SQL — query DB lazy. -->
# var là gì?
# dynamic là gì?
<!-- # var và dynamic khác nhau gì?
`var`: type được infer lúc compile, sau đó cố định — vẫn type-safe. `dynamic`: bypass compile-time check, type resolve lúc runtime — linh hoạt nhưng dễ lỗi runtime. -->

<!-- # delegate và event khác nhau gì?
Delegate là type chứa pointer đến method. Event là wrapper quanh delegate — bên ngoài chỉ `+=`/`-=` được, không invoke hay overwrite trực tiếp. Dùng cho publisher/subscriber. -->
# struct là gì?
# struct được dùng làm gì?
<!-- # struct và class khác nhau gì?
`struct` là value type, copy bằng giá trị, thường ở stack, nhẹ. `class` là reference type, ở heap, hỗ trợ kế thừa. Struct nên giữ nhỏ và immutable. -->

# SOLID là gì?
là 5 nguyên tắc OOP: 
**S**ingle Responsibility, 
**O**pen/Closed, 
**L**iskov Substitution, 
**I**nterface Segregation, 
**D**ependency Inversion. 
# lí do OOP tồn tại?
Giúp code dễ maintain, test, extend.

# Singleton, Scoped, Transient khác nhau gì?
- `Singleton`: vòng đời giống app.
- `Scoped`: vòng đời giống request.
- `Transient`: vòng đời giống hàm dùng nó

# pipeline là gì?
# trong MinimalAPI có pipeline không?
# idisposable và using luôn đi cùng nhau phải không?
# khi dùng using thì k cần gọi dispose à? tại sao?
# trường hợp nào cần dùng dispose?
# IDisposable và using dùng để làm gì?
`IDisposable` interface cho object nắm giữ resource cần dọn (file, connection, DB). `using` tự gọi `Dispose()` khi thoát scope, kể cả khi có exception.