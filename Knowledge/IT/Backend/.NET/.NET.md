---
id: 311
name: ".NET"
---

# .NET là gì? [id:2690 order:1]
là nền tảng phát triển phần mềm của Microsoft, gồm runtime (CLR) và thư viện chuẩn (BCL), dùng để build app console, web, desktop, mobile.

# CLR là gì? [id:2691 order:2]
là runtime của .NET
, chịu trách nhiệm biên dịch IL sang machine code (JIT), quản lý bộ nhớ (GC), và xử lý exception.

<!--# runtime gồm những gì? [id:2692 order:3] -->

# IL là gì? [id:2693 order:4]
là Intermediate Language — bytecode mà compiler C# tạo ra. CLR sẽ JIT compile IL thành machine code lúc runtime.

# compiler tạo ra gì? [id:2694 order:5]

# CLR tạo ra gì? [id:2695 order:6]

# JIT là gì? [id:2696 order:7]
là quá trình CLR biên dịch IL sang machine code

# GC trong .NET là gì? [id:2697 order:8]
là Garbage Collector — tự động giải phóng memory của object không còn được tham chiếu, không cần dev tự `free()`.

# namespace trong .NET là gì? [id:2698 order:9]
là 1 nhóm các class/interface

# assembly là gì? [id:2699 order:10]
là đơn vị deploy của .NET — thường là file `.dll` hoặc `.exe`, chứa IL và metadata của 1 project hoặc library.

# cho tôi vài câu phổ biến có dùng assemply? [id:2700 order:11]

# interface khác abstract class thế nào trong C#? [id:2701 order:12]
- Interface: chỉ định nghĩa contract (method/property), không có implementation (từ C# 8 có default impl)
- Abstract class: có thể có implementation, chỉ kế thừa được 1 class, thêm trạng thái (field)

# contract có nghĩa là gì? [id:2702 order:13]

# sự khác nhau giữa "định nghĩa" và triển khai? [id:2703 order:14]

# IDisposable và using statement dùng để làm gì? [id:2704 order:15]
`IDisposable` là interface để giải phóng tài nguyên unmanaged (file, connection, socket). `using` block tự gọi `Dispose()` khi ra khỏi scope.

# tài nguyên unmanaged nghĩa là gì? [id:2705 order:16]

# dispose là gì? [id:2706 order:17]

# khi nào dùng async/await gì? [id:2707 order:18]

# Task và Thread khác nhau thế nào? [id:2708 order:19]
Thread là đơn vị OS thực sự, tốn tài nguyên. Task là abstraction cao hơn, nhiều Task chạy trên ít thread qua thread pool — nhẹ hơn và dễ compose hơn.

# đơn vị OS là gì? [id:2709 order:20]

# abstraction nghĩa là gì? [id:2710 order:21]

# compose là gì? [id:2711 order:22]

# dependency injection là gì? [id:2712 order:23]
là 1 pattern

# DI giải quyết vấn đề gì? [id:2713 order:24]

# DI có phải là technique không? [id:2714 order:25]

# singleton, scoped, transient là gì? [id:2715 order:26]

# Singleton, Scoped, Transient khác nhau thế nào? [id:2716 order:27]
- Singleton: 1 instance dùng suốt lifetime app
- Scoped: 1 instance mỗi HTTP request
- Transient: tạo mới mỗi lần inject

# middleware trong ASP.NET Core là gì? [id:2717 order:28]
là thành phần xử lý HTTP request/response theo pipeline.

# ware trong middleware có nghĩa là phần mềm phải không?? [id:2718 order:29]

# circuit là gì? [id:2719 order:30]

# IHostedService là gì? [id:2720 order:31]
là interface để chạy background task

# Entity Framework Core là gì? [id:2721 order:32]
là ORM của .NET

# ORM là gì? [id:2722 order:33]

# LINQ là gì? [id:2723 order:34]

# appsettings.json dùng để làm gì? [id:2724 order:35]
là file config của ASP.NET Core app

# appsettings có bị overwrite không? [id:2725 order:36]

# một .net server đơn giản nhất gồm những gì? [id:2726 order:37]

# web server của .net là gì? [id:2727 order:38]

# kestrel có phải là http server không? [id:2728 order:39]

# mỗi middleware là 1 class độc lập à? [id:2729 order:40]

# request đi qua những gì trước khi đến db? [id:2730 order:41]