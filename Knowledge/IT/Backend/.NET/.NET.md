---
id: 311
name: ".NET"
---

# .NET là gì? [id:2690 order:1]
là nền tảng phát triển phần mềm của Microsoft, gồm runtime (CLR) và thư viện chuẩn (BCL), dùng để build app console, web, desktop, mobile.

# namespace trong .NET là gì? [id:2797 order:2]
là 1 nhóm các class/interface

# kế thừa là gì? [id:3133 order:3]
là con thừa hưởng member của cha

# lamda expenssion gì? [id:3255 order:4]
là arrow function của .NET

# từ delegate có mấy nghĩa? [id:3256 order:5]
2 nghĩa:
- là function type.
- là instance của function type

# delegate giờ ít phổ biến à? vì sao? [id:3257 order:6]
đúng, vì giờ hầu hết dùng Action
Ít dùng trực tiếp. Thay vào đó dùng `Action`, `Func<T>` (built-in delegate) hoặc lambda trực tiếp. Từ khoá `delegate` để khai báo type mới thì hiếm — chỉ khi cần tên rõ ràng hơn `Action`/`Func`.

# core mechanic là gì? [id:3295 order:7]
là cơ chế hoạt động cốt lõi

# mechanic phát âm? [id:3296 order:8]
/məˈkænɪk/

# gotcha là gì? ví dụ? [id:3297 order:9]
là hành vi không trực quan
của ngôn ngữ/framework mà dev dễ mắc lỗi nếu không biết trước. Ví dụ: `new HttpClient()` mỗi request → cạn port (gotcha kinh điển trong .NET).

# tại sao new HttpClient() lại cạn port nhỉ? [id:3618 order:10]

# sequential là gì? [id:3384 order:11]
tuần tự — thực hiện từng bước một,
bước sau chỉ bắt đầu khi bước trước xong.

# parallel là gì? [id:3385 order:12]
là chạy song song
— nhiều việc chạy cùng lúc, không chờ nhau.

# parallel phát âm? [id:3619 order:13]
/ˈpærəlel/

# Task.WhenAny hoạt động thế nào? [id:3386 order:14]
trả về ngay khi có **một** task hoàn thành đầu tiên (không cancel các task còn lại).

# Retry external service là pattern à? [id:3387 order:15]
Đúng — "Retry with exponential backoff" là pattern chuẩn khi gọi external service (DB, API, queue).

<!--# Retry with exponential backoff pattern giải quyết vấn đề gì? [id:3388 order:16]
external service có thể tạm thời unavailable (transient failure).
Retry với delay tăng dần (200ms → 400ms → 800ms)
->> tránh hammer server khi nó đang phục hồi. -->

# So sánh ReadAllTextAsync và StreamReader? [id:3389 order:17]
`File.ReadAllTextAsync` load **toàn bộ** file vào RAM một lúc
`StreamReader` đọc từng chunk

<!--# tại sao new liên tục lại hết port? [id:3390 order:18] -->